services:
  guindex:
    build: .
    ports:
      - "${PORT:-7000}:7000"
    env_file:
      - .env
    environment:
      - PORT=7000
      - LOG_LEVEL=info
      # Self-host tuning (opcional):
      # - TORRENT_INDEXER_ENABLE_FALLBACK=true
      # - TORRENT_INDEXER_SEARCH_CACHE_TTL_MS=120000
      # - TORRENT_INDEXER_INDEXERS_CACHE_TTL_MS=600000
      # - TORRENT_INDEXER_FALLBACK_MAX_INDEXERS=0  # 0 = consulta todos os indexers
      # - TORRENT_INDEXER_FALLBACK_PER_INDEXER_LIMIT=0  # 0 = sem limite na query
      # - TORRENT_INDEXER_FALLBACK_CONCURRENCY=3
      # - TORRENT_INDEXER_MAX_QUERY_TIME_MS=15000
      # - TORRENT_INDEXER_TARGET_STREAMS=12
      # - TORRENT_INDEXER_MAX_DYNAMIC_QUERIES=10
      # - TORRENT_INDEXER_DISABLED_INDEXERS=comando_torrents
      # - TORRENT_INDEXER_FAILURE_THRESHOLD=2
      # - TORRENT_INDEXER_FAILURE_COOLDOWN_MS=900000
      # OBRIGATÓRIO: defina o domínio público da sua instância
      # - BASE_URL=https://seu-dominio.com
    volumes:
      - ./public:/app/public:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:7000/health', (res) => { process.exit(res.statusCode === 200 ? 0 : 1) }).on('error', () => process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
